import type { SiteConfiguration, SocialLinks } from "@/types.ts";

export const SITE: SiteConfiguration = {
  name: "Trevor Tyler Lee",
  bio: "LinkTree alternative built with Astro & Tailwind CSS.",
  url: "https://astro-link.vercel.app",
  locale: "en-CA",
};
